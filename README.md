# Aretas updates

Sparkle update feed for the Aretas macOS app. `appcast.xml` + release DMGs are
published here by `app/scripts/release.sh` in the main repo. The feed is
EdDSA-signed; hosting is not a trust boundary.
